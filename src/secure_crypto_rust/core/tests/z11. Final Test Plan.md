# Test

Here’s a structured **final‑stage test plan** for our public API (`encrypt_stream_v2`, `decrypt_stream_v2`). The goal is to validate correctness, robustness, and telemetry behavior across all supported input/output abstractions and configurations.

---

## 🧪 Test Categories

### 1. **Round‑Trip Correctness**

- **Minimal plaintext**: Encrypt/decrypt a short string (`b"hello world"`) and assert equality.
- **Large plaintext**: Encrypt/decrypt multi‑MB input to stress chunking and parallelism.
- **Exact chunk boundaries**: Input size exactly equal to `HeaderV1.chunk_size` × N.
- **Empty input**: Expect `StreamError::SegmentWorker` or equivalent error.

#### 🧪 Test File: `secure_crypto_rust/core/tests/public_api/test_roundtrip.rs`

### 2. **InputSource Variants**

- **Memory**: `InputSource::Memory(Vec<u8>)`.
- **File**: Write plaintext to a temp file, use `InputSource::File`, assert decrypted output matches.
- **Reader**: Wrap a `Cursor<Vec<u8>>` in `InputSource::Reader`.

#### 🧪 Test File: `secure_crypto_rust/core/tests/public_api/test_input_variants.rs`

### 3. **OutputSink Variants**

- **Memory**: `OutputSink::Memory` with `ApiConfig.with_buf = Some(true)` → assert `snapshot.output` contains ciphertext.
- **File**: Write ciphertext to file, then decrypt from file.
- **Writer**: Use `Cursor<Vec<u8>>` wrapped in `OutputSink::Writer`.

#### 🧪 Test File: `secure_crypto_rust/core/tests/public_api/test_output_variants.rs`

### 4. **Header & Metadata Validation**

- **Header presence**: Assert ciphertext begins with valid `HeaderV1.magic` and length ≥ `HeaderV1::LEN`.
- **Flags**: Construct headers with different flags (`HAS_TOTAL_LEN`, `HAS_CRC32`, etc.) and assert they are preserved.
- **AAD domain mismatch**: Corrupt header field → expect `StreamError::Header`.

#### 🧪 Test File: `secure_crypto_rust/core/tests/public_api/test_header_meta.rs`

### 5. **Corruption Detection**

- **Bit‑flip in ciphertext**: Modify a byte inside a segment → expect `StreamError::SegmentWorker`.
- **Header corruption**: Flip a byte in header → expect `StreamError::Header`.

#### 🧪 Test File: `secure_crypto_rust/core/tests/public_api/test_corrupt_detection.rs`

### 6. **Parallelism Profiles**

- **Single‑threaded**: `HybridParallelismProfile::single_threaded()`.
- **Multi‑threaded**: `HybridParallelismProfile::new(4,4,8)` → assert decrypted output matches plaintext.
- **Extreme backpressure**: Very large input with small worker counts → ensure no deadlock.

#### 🧪 Test File: `secure_crypto_rust/core/tests/public_api/test_parallelism_profiles.rs`

### 7. **Telemetry & ApiConfig**

- **with_buf = Some(true)**: Assert `snapshot.output` contains ciphertext.
- **with_buf = None**: Assert `snapshot.output` is `None`.
- **collect_metrics = Some(true)**: (future‑proof) ensure snapshot still returns counters, even if unused.

#### 🧪 Test File: `secure_crypto_rust/core/tests/public_api/test_telemetry_config.rs`

### 8. **Compression & Cipher Variants**

- **Compression disabled**: `codec_ids::AUTO`.
- **Compression enabled**: `codec_ids::ZSTD` or `codec_ids::LZ4`.
- **Cipher variants**: `cipher_ids::AES256_GCM` vs `cipher_ids::CHACHA20_POLY1305`.
- **PRF variants**: `prf_ids::SHA256`, `prf_ids::BLAKE3K`.

#### 🧪 Test File: `secure_crypto_rust/core/tests/public_api/test_compression_cipher_variants.rs`

---

## ✅ Summary

Final stage tests should cover:

- **Correctness** (round‑trip, chunk boundaries, empty input).
- **All input/output abstractions**.
- **Header validation & corruption detection**.
- **Parallelism profiles & stress tests**.
- **Telemetry buffer extraction via `ApiConfig.with_buf`**.
- **Cipher/PRF/compression variants**.

This ensures our public API is validated across functional, robustness, and telemetry dimensions.  

---

Here’s a clear **matrix table of test cases** that systematically covers our public API (`encrypt_stream_v2`, `decrypt_stream_v2`). Rows represent **input/output variants**, columns represent **profiles/configs**. This way we can see at a glance which combinations are exercised.

---

## 📊 Test Coverage Matrix

| **Input / Output Variant** | **Single‑Threaded Profile** | **Multi‑Threaded Profile** | **Extreme Backpressure Profile** | **ApiConfig.with_buf = None** | **ApiConfig.with_buf = Some(true)** |
|-----------------------------|-----------------------------|-----------------------------|----------------------------------|-------------------------------|-------------------------------------|
| **Memory → Memory**         | ✅ Round‑trip correctness (small plaintext) | ✅ Large plaintext (multi‑MB) | ✅ Stress test (128 MB, small workers) | ✅ Production default (no buffer captured) | ✅ Telemetry buffer captured, assert `snapshot.output` |
| **Memory → File**           | ✅ Round‑trip correctness (write ciphertext to file, read back) | ✅ Parallel correctness | ✅ Stress test with file sink | ✅ Default file output | N/A (buffer capture not relevant for file sink) |
| **Memory → Writer(Cursor)** | ✅ Round‑trip correctness | ✅ Parallel correctness | ✅ Stress test | ✅ Default writer output | N/A |
| **File → Memory**           | ✅ Round‑trip correctness (plaintext from file) | ✅ Parallel correctness | ✅ Stress test | ✅ Default | ✅ Telemetry buffer captured |
| **File → File**             | ✅ Round‑trip correctness | ✅ Parallel correctness | ✅ Stress test | ✅ Default | N/A |
| **Reader(Cursor) → Memory** | ✅ Round‑trip correctness | ✅ Parallel correctness | ✅ Stress test | ✅ Default | ✅ Telemetry buffer captured |
| **Reader(Cursor) → File**   | ✅ Round‑trip correctness | ✅ Parallel correctness | ✅ Stress test | ✅ Default | N/A |

---

## 🧩 Notes

- **Profiles**:  
  - *Single‑Threaded*: `HybridParallelismProfile::single_threaded()`.  
  - *Multi‑Threaded*: e.g. `HybridParallelismProfile::new(4,4,8)`.  
  - *Extreme Backpressure*: large input with small worker counts (e.g. `HybridParallelismProfile::new(8,8,1)`).

- **ApiConfig**:  
  - `with_buf = None` → production default, no telemetry buffer.  
  - `with_buf = Some(true)` → test mode, capture ciphertext in `snapshot.output`.  

- **Corruption/Validation Tests**:  
  - Overlay corruption tests (bit‑flip in ciphertext, header mismatch) across **Memory → Memory** rows with both single‑threaded and multi‑threaded profiles.  
  - These don’t require file/reader variants, but we can extend if desired.

---

This matrix ensures we systematically cover:

- All **input/output abstractions** (`Memory`, `File`, `Reader`).
- All **parallelism profiles** (single, multi, extreme).
- Both **ApiConfig modes** (with/without buffer capture).

---

## ▶️ Running Tests

Since we have a **Cargo workspace**:

- To run **all tests in the workspace**:

  ```bash
  cargo test
  ```

- To run **all tests inside the `core` crate**:

  ```bash
  cargo test -p core
  ```

- To run **all tests inside the `public_api` folder**:

  ```bash
  cargo test -p core --test public_api
  ```

  Explanation:
  - `--test public_api` runs the integration test target named `public_api`.  
  - Cargo treats each file in `core/tests/public_api/` as part of the `public_api` integration test suite.

- To run a **specific test file**:

  ```bash
  cargo test -p core --test test_roundtrip
  ```

- To run a **specific test function**:

  ```bash
  cargo test -p core roundtrip_minimal_plaintext
  ```

---

this is my project structure:

1. secure_crypto_rust/core/tests/test_*.rs
2. secure_crypto_rust/core/tests/public_api/test_*.rs

- Number 2, tests inside subfolder (public_api) are not compiling by vs_code, so I can't fix error automatically
- Why, it is behaving like this?
- Do I need to add anything to my Cargo.toml


- Give me plan for final stage tests of our public api (encrypt_stream_v2, decrypt_stream_v2)

/// Canonical input abstraction
pub enum InputSource {
    Reader(Box<dyn Read + Send>),
    File(PathBuf),
    Memory(Vec<u8>),
}

/// Canonical output abstraction
pub enum OutputSink {
    Writer(Box<dyn Write + Send>),
    File(PathBuf),
    Memory,
}

#[derive(Clone, Debug)]
pub struct EncryptParams<'a> {
    pub header: HeaderV1,
    pub dict: Option<&'a [u8]>,
}

#[derive(Clone, Debug)]
pub struct DecryptParams;
impl DecryptParams {
    pub fn validate(&self) -> Result<(), StreamError> {
        Ok(())
    }
}

#[derive(Debug, Clone)]
pub struct ApiConfig {
    /// Whether to capture the output buffer in memory.
    /// - `None` or `Some(false)` → no buffer capture (production default).
    /// - `Some(true)` → capture buffer for tests/benchmarks.
    pub with_buf: Option<bool>,

    /// Whether to collect detailed metrics during pipeline execution.
    /// Currently unused, reserved for future expansion.
    pub collect_metrics: Option<bool>,
}

pub mod codec_ids {
    pub const AUTO: u16    = 0x0000;
    pub const ZSTD: u16    = 0x0001;
    pub const LZ4: u16     = 0x0002;
    pub const DEFLATE: u16 = 0x0003;
}

/// Cipher suite identifiers (mirrored in headers).
pub mod cipher_ids {
    pub const AES256_GCM: u16        = 0x0001;
    pub const CHACHA20_POLY1305: u16 = 0x0002;
}

/// HKDF PRF identifiers (mirrored in headers).
pub mod prf_ids {
    pub const SHA256: u16  = 0x0001;
    pub const SHA512: u16  = 0x0002;
    pub const SHA3_256: u16  = 0x0003;
    pub const SHA3_512: u16  = 0x0004;
    pub const BLAKE3K: u16 = 0x0005; // keyed BLAKE3 (avoid unless policy requires)
}

/// Flag bitmask for optional features and metadata presence.
pub mod flags {
    pub const HAS_TOTAL_LEN: u16    = 0x0001;
    pub const HAS_CRC32: u16        = 0x0002;
    pub const HAS_TERMINATOR: u16   = 0x0004;
    pub const HAS_FINAL_DIGEST: u16 = 0x0008;
    pub const DICT_USED: u16        = 0x0010;
    pub const AAD_STRICT: u16       = 0x0020;
}

#[repr(C)]
#[derive(Debug, Copy, Clone, PartialEq)]
pub struct HeaderV1 {
    pub magic: [u8; 4],        // "RSE1" magic marker
    pub version: u16,          // protocol version
    pub alg_profile: u16,      // bundle id (cipher + PRF choice)
    pub cipher: u16,           // cipher enum (cipher_ids)
    pub hkdf_prf: u16,         // PRF enum (prf_ids)
    pub compression: u16,      // compression enum (codec_ids)
    pub strategy: u16,         // sequential / parallel / auto
    pub aad_domain: u16,       // binds header semantics in AAD
    pub flags: u16,            // presence and behavior bits
    pub chunk_size: u32,       // frame plaintext target size
    pub plaintext_size: u64,   // optional; 0 if unknown
    pub crc32: u32,            // optional; 0 if not provided
    pub dict_id: u32,          // optional compression dictionary id
    pub salt: [u8; 16],        // nonce base (random per stream)
    pub key_id: u32,           // master key registry reference
    pub parallel_hint: u32,    // optional suggested worker count
    pub enc_time_ns: u64,      // optional monotonic encoding timestamp
    pub reserved: [u8; 8],     // future fields; must be zero
}

/// 🔐 Encrypt stream (v2)
pub fn encrypt_stream_v2(
    input: InputSource,
    output: OutputSink,
    master_key: &[u8],
    params: EncryptParams,
    config: ApiConfig, // new param
) -> Result<TelemetrySnapshot, StreamError> {
    validate_encrypt_params(master_key, &params, None, None)?;

    let reader = open_input(input)?;
    let (writer, maybe_buf) = open_output(output, config.with_buf)?;

    // ---- Read stream header ----
    let mut payload_reader = PayloadReader::new(reader);

    let (mut crypto, profile, log_manager) = setup_enc_context(master_key, &params.header, DigestAlg::Blake3)?;
    let config_pipe = PipelineConfig::new(profile, maybe_buf.clone());

    let mut snapshot = run_encrypt_pipeline(
        &mut payload_reader,
        writer,
        &mut crypto,
        &config_pipe,
        log_manager,
    )?;

    // --- Telemetry buffer extraction for tests --- 
    if let Some(ref arc_buf) = maybe_buf { 
        let buf = arc_buf.lock().unwrap(); 
        snapshot.attach_output(buf.clone()); 
        // clone Vec<u8> into snapshot.output 
    }

    Ok(snapshot)
}

/// 🔓 Decrypt stream (v2)
pub fn decrypt_stream_v2(
    input: InputSource,
    output: OutputSink,
    master_key: &[u8],
    params: DecryptParams,
    config: ApiConfig, // new param
) -> Result<TelemetrySnapshot, StreamError> {
    //
    validate_decrypt_params(master_key, &params, None, None)?;

    let reader = open_input(input)?;
    let (writer, maybe_buf) = open_output(output, config.with_buf)?;

    // ---- Read stream header ----
    // Assert reader is positioned correctly
    let (header, mut payload_reader) = PayloadReader::with_header(reader)?;

    let (mut crypto, profile, log_manager) = setup_dec_context(master_key, &header, DigestAlg::Blake3)?;
    let config_pipe = PipelineConfig::new(profile, maybe_buf.clone());

    let mut snapshot = run_decrypt_pipeline(
        &mut payload_reader,
        writer,
        &mut crypto,
        &config_pipe,
        log_manager,
    )?;

    // --- Telemetry buffer extraction for tests --- 
    if let Some(ref arc_buf) = maybe_buf { 
        let buf = arc_buf.lock().unwrap(); 
        snapshot.attach_output(buf.clone()); 
        // clone Vec<u8> into snapshot.output 
    }

    Ok(snapshot)
}

